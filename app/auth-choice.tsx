import { View, Text } from "react-native";
import React from "react";

const AuthChoice = () => {
  return (
    <View className="bg-white h-full">
      <Text className="text-dark-400">AuthChoice</Text>
    </View>
  );
};

export default AuthChoice;
